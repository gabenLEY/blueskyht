import Layout from "../layout/Layout"
import Profile from "./Profile"
import LayoutApp from "../layout/LayoutApp"

const ProfileContainer = () => {
  return (
    <Layout>
      <LayoutApp><Profile /></LayoutApp>
    </Layout>
    
  )
}

export default ProfileContainer